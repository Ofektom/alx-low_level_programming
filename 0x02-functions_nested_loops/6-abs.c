#include "main.h"

/**
 * _abs - Function for absolute number
 * @r: Integer inout
 * Desc: This function returns absolute value of a number
 * Return: Absolut value of r
 */
int _abs(int r)
{
if (r >= 0)
	return (r);
else
	return (r * -1);
}
